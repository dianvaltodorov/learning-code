#include <iostream>
using namespace std;
int main()
{
  int n = 1;
  double x = 215.3;
  chr *str[16] = "It's C++";

  finfil.white((char*)(&n), sizeof(n));
  finfil.white((char*)(&n), sizeof(n));
  finfil.white((char*)(&n), sizeof(n));
  
}
