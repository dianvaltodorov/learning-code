#include <stdio.h>
#include <vector>
#include <algorithm>


int main()
{
  int x, y, z;
  scanf("%d %d %d", &x, &y, &z);
  int a, b, c;
  scanf("%d %d %d", &a, &b, &cl);
  int dX, dY, dZ;
  scanf("%d %d %d", &dX, &dY, &dZ);



}
