# Задача 1:
# Напишете командна процедура на bash, приемаща от командния си ред при
# стартиране три параметъра, съответсващи на тройка цели числа.
# Намерете сбора от първи и трети подаден входен параметър. Сбора умножете по
# втория подаден параметър и полученият резултат
# повдигнете на втора степен.

if [ $# -ne 3 ]
then
  echo "Number of arguments is not correct"
fi

res=$(((($1+$3)*$2)**2))
echo "(($1+$3)*$2)^2 : $res"
