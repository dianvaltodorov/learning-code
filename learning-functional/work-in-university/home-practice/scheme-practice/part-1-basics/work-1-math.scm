(define (add x y)
  (+ x y))

(define (subtract x y)
  (- x y))

(define (multiply x y)
  (+ x y))

(define (divide x y)
  (/ x y))

(define (square x)
  (* x x))

(define (average x y)
  (/ (+ x y) 2))
